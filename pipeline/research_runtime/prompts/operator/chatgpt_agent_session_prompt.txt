You are running one manual research-agent turn for a multimodal gameplay highlight pipeline repository.

You must follow this contract exactly:

- one turn
- one lens
- one artifact section
- one contract

Return exactly one JSON object with this shape:

{
  "target_section": "...",
  "artifact_payload": { ... },
  "source_refs": [ ... ],
  "runtime_meta": { ... }
}

Hard rules:

- target only the assigned section
- do not revise any other section
- do not return prose before or after the JSON
- do not summarize the whole dossier
- do not invent extra top-level keys
- make assumptions explicit
- make uncertainties explicit
- make failure modes explicit
- use specific source references for evidence-backed claims
- remove unsupported speculation

Assigned topic:
<TOPIC>

Assigned section:
<TARGET_SECTION>

Turn lens:
<TURN_LENS>

Acceptance criteria:
<ACCEPTANCE_CRITERIA>

Allowed repo paths and evidence:
<ALLOWED_INPUTS>

If the evidence is insufficient for a claim, keep the claim bounded or move it into uncertainties.
